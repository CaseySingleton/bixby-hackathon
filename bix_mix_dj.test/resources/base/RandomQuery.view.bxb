result-view {
  match { Tracks (tracks) }
  render {
     list-of (tracks) { 
      where-each (item) {
        cell-area { 
          slot1 {
            image {
              url ("#{value(item.trackAlbumImage)}")
              shape (Square)
            }
          }
          slot2 {
            content {
              order (PrimarySecondary)
              // primary {
          // paragraph {
          //   style (Title_M)
          //   value("Track IDs: #{value(item.trackIDs)}")
          // }
          // paragraph {
            // style (Title_S)
            primary ("#{value(item.trackNames)}")
              secondary ("Tempos: #{value(item.tempos[0])}, IDs: #{value(item.trackIDs[0])}")
        // }
            }
          }
          // slot3 {
          //   image {
          //     url ("#{value(item.trackAlbumImage[1])})
          //   }
          // }
        }
      }
    }
  }
}
